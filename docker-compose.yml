version: '3.8'

services:
  frontend:
    build:
      context: ./App
    ports:
      - "80:80"

  backend:
    build:
      context: .
      dockerfile: Server/Dockerfile
    volumes:
       - ./Database:/Database 
    ports:
      - "8080:8080"
